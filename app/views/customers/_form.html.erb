<%= form_for(@customer) do |f| %>
  <% if @customer.errors.any? %>
    <div id="error_explanation">
      <h3><%= pluralize(@customer.errors.count, "error") %> prohibited this customer from being saved:</h3>

      <ul>
      <% @customer.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :name %><br>
    <%= f.text_field :name, class: 'form-control', placeholder: '...', required: true %>
  </div>
  <div class="form-group">
    <%= f.label :DatevId %><br>
    <%= f.number_field :name, class: 'form-control', placeholder: '...', required: true %>
  </div>
  <div class="form-group">
    <%= f.label :displayname %><br>
    <%= f.text_field :displayname, class: 'form-control', placeholder: '...', required: true %>
  </div>
  <div class="form-group">
    <%= f.label :solution_architect_id, 'Solution Architect' %><br>
    <%= f.collection_select(:solution_architect_id, SolutionArchitect.all.order(lastname: :asc), :id, :lastname, {}, {:class=> 'form-control'}) %>
  </div>
  <div class="form-group actions">
    <%= f.submit 'Speichern', :class => 'btn btn-lg btn-success btn-block' %>
  </div>
<% end %>
